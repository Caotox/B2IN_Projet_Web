server {
    listen 80;
    server_name localhost; # Remplacer par notre nom de domaine

    location / {
        root path; # Remplacer par le chemin vers index.html
        index index.html;
    }

    location /api/ {
        proxy_pass http://localhost:3000/; # Redirige les requêtes API vers ton serveur backend (API REST)
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
